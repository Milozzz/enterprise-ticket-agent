# 企业 Agent 人工与外部环境验证计划

更新日期：2026-07-14

## 1. 目的

本文件记录代码仓库无法单独证明、但可以通过人工操作、外部服务或真实用户补充验证的项目能力。

作为独立开发者，不需要为了作品项目强行寻找 SAP 合作公司。验证时应明确区分以下三个层级：

1. **代码与自动化测试已验证**：可以由测试、Eval、Simulation 和本地环境重复证明。
2. **契约级或独立环境已验证**：使用官方 API 定义、公共 Sandbox、真实云服务或人工演练证明。
3. **企业生产环境待验证**：需要真实 S/4HANA 租户、企业身份体系、正式业务流量或合规团队。

面试和简历中只能陈述已经实际完成的层级，不能把 Mock、Shadow Write 或本地演练描述成企业生产验证。

## 2. 推荐优先级

| 优先级 | 验证项目 | 个人完成难度 | 成本 | 建议 |
| --- | --- | ---: | ---: | --- |
| P0 | SAP Accelerator Hub Sandbox 读取 | 低 | 基本免费 | 完成 |
| P0 | SAP `$metadata` 与字段映射 | 低 | 免费 | 完成 |
| P0 | Render 单实例真实压测 | 低 | 免费或低成本 | 完成 |
| P0 | 本地双实例一致性测试 | 中 | 免费 | 完成 |
| P0 | PostgreSQL 备份恢复演练 | 低 | 免费 | 完成 |
| P1 | 真实 KMS 加密与轮换演练 | 中 | 低用量成本 | 推荐完成 |
| P1 | 3-5 名目标用户 Shadow Pilot | 中 | 基本免费 | 推荐完成 |
| P2 | Render 付费多副本和自动扩缩容 | 中 | 需要付费 | 最终发布前短期开通 |
| 外部依赖 | SAP 真实 Credit Memo 写入 | 高 | 依赖企业测试租户 | 暂缓 |
| 外部依赖 | Principal Propagation | 很高 | 依赖 BTP、IdP 和 S/4HANA | 暂缓 |
| 外部依赖 | 多区域灾备与正式合规认证 | 高 | 成本较高 | 商业化后推进 |

## 3. P0：SAP 契约级验证

### 3.1 验证目标

在没有企业 S/4HANA 租户的情况下，证明项目可以理解和消费真实 SAP OData 契约，而不是只连接自定义 Mock API。

### 3.2 操作步骤

- [ ] 注册并登录 SAP Business Accelerator Hub。
- [ ] 获取 Sandbox API Key。
- [ ] 选择订单、Business Partner、Customer Return 和 Credit Memo Request 等相关 API。
- [ ] 下载 OpenAPI、EDMX 或 `$metadata`。
- [ ] 配置 `SAP_CONNECTOR_MODE=live`。
- [ ] 配置 `SAP_AUTH_TYPE=api_key` 和 `SAP_API_KEY`。
- [ ] 保持 `SAP_READ_ONLY=true`。
- [ ] 保持 `SAP_SHADOW_WRITES=true`。
- [ ] 调用真实 Sandbox 读取接口。
- [ ] 运行项目已有的 SAP Sandbox smoke 脚本。
- [ ] 验证分页、过滤、排序、expand 和 metadata。
- [ ] 验证 400、401、403、404、429 和 SAP 标准错误结构。
- [ ] 建立 SAP 字段到 Canonical Data Model 的映射表。

示例环境变量：

```text
SAP_CONNECTOR_MODE=live
SAP_AUTH_TYPE=api_key
SAP_API_KEY=<sandbox-api-key>
SAP_BASE_URL=<sandbox-base-url>
SAP_READ_ONLY=true
SAP_SHADOW_WRITES=true
```

示例命令：

```powershell
cd backend
python scripts/sap_sandbox_smoke.py --order-id <sandbox-order-id>
```

### 3.3 应保存的证据

- [ ] 脱敏后的 HTTP 请求与响应。
- [ ] 真实 `$metadata` 或 EDMX 文件摘要。
- [ ] SAP 字段到内部 Canonical Model 的映射表。
- [ ] SAP 错误响应样本。
- [ ] Connector acceptance JSON 报告。
- [ ] 测试时间、API 名称、版本和环境说明。
- [ ] Read Only 与 Shadow Write 配置截图。

### 3.4 验收标准

- [ ] 至少一个真实 SAP Sandbox 读取接口成功。
- [ ] 返回结构通过项目 Schema 校验。
- [ ] 字段映射可以生成项目内部统一订单或业务对象。
- [ ] 错误响应被转换成统一 Connector Error，而不是未处理异常。
- [ ] 没有向 Sandbox 或未知系统提交真实写操作。

### 3.5 可用于简历的表述

> 完成 SAP S/4HANA OData API 契约级集成，基于官方 Sandbox 验证真实 metadata、读取接口、错误结构和字段映射；写操作通过 Shadow Mode 与契约测试验证，尚未接入企业生产租户。

### 3.6 当前不能宣称的内容

- 未获得真实 S/4HANA 测试租户时，不能宣称真实创建了 Credit Memo Request。
- 未完成 BTP、IdP、Destination 和用户映射时，不能宣称 Principal Propagation 已在生产环境通过。
- SAP 公共 Sandbox 验证不等于企业生产系统集成。

官方资料：[SAP Business Accelerator Hub API Details](https://help.sap.com/docs/business-accelerator-hub/sap-business-accelerator-hub/api-details?version=Cloud)

## 4. P0：本地双实例一致性验证

### 4.1 验证目标

不购买 Render 多实例，先在本地证明后端无本地业务状态依赖，两个实例可以共享 PostgreSQL、Redis、Checkpoint 和任务队列。

建议结构：

```text
Nginx / Load Balancer
  |-- Backend Instance 1
  `-- Backend Instance 2
          |
          |-- PostgreSQL
          `-- Redis
```

### 4.2 操作步骤

- [ ] 启动两个独立后端实例。
- [ ] 两个实例连接同一 PostgreSQL 和 Redis。
- [ ] 通过 Nginx 或其他负载均衡入口访问。
- [ ] 在实例 1 触发 HITL 中断。
- [ ] 让审批恢复请求进入实例 2。
- [ ] 对同一个 `Idempotency-Key` 并发提交 50 次。
- [ ] 启动两个 Worker，验证同一任务只被一个 Worker 获取。
- [ ] 在任务执行期间停止实例 1。
- [ ] 验证实例 2 可以读取任务状态并安全恢复。
- [ ] 验证本地文件系统清空后业务记录不丢失。

### 4.3 验收标准

- [ ] HITL 能跨实例恢复。
- [ ] 并发幂等写操作只产生一个业务结果。
- [ ] 同一 Agent Job 不会被两个 Worker 同时执行。
- [ ] 一个实例停止不会导致数据库业务状态丢失。
- [ ] Redis 临时丢失不会删除 PostgreSQL 中的最终业务事实。

### 4.4 应保存的证据

- [ ] 两个实例 ID 的日志。
- [ ] 相同 `thread_id` 跨实例执行记录。
- [ ] 幂等并发测试报告。
- [ ] Worker claim 记录。
- [ ] 实例停止和恢复时间线。

## 5. P0：Render 单实例真实压测

### 5.1 验证目标

证明线上 Render 环境的真实冷启动、HTTP 延迟、Agent 首结果时间、端到端时间和错误率。

### 5.2 建议测试矩阵

| 测试组 | 并发用户 | 持续时间 | 说明 |
| --- | ---: | ---: | --- |
| 冷启动 | 1 | 多轮 | 空闲后首个请求 |
| 基础负载 | 5 | 10 分钟 | 查询和健康检查 |
| Agent 负载 | 10 | 10-20 分钟 | SSE、审批和只读流程 |
| 幂等压力 | 20-50 个并发请求 | 单轮 | 相同 Idempotency-Key |

示例命令：

```powershell
cd backend
locust -f locustfile.py `
  --host https://enterprise-ticket-agent-backend.onrender.com `
  --headless -u 10 -r 2 -t 10m
```

### 5.3 必须记录的指标

- [ ] 冷启动时间。
- [ ] HTTP P50、P95 和 P99。
- [ ] Agent TTFR P50、P95 和 P99。
- [ ] Agent 端到端完成时间。
- [ ] 5xx、429、超时和断流比例。
- [ ] CPU、内存、数据库连接和 Redis 连接。
- [ ] 每个成功任务的 Token 与成本。
- [ ] 人工审批等待时间与恢复耗时。

### 5.4 验收标准

- [ ] 测试过程中没有数据重复写入。
- [ ] 失败请求具备明确错误类型和 trace ID。
- [ ] SSE 能产生首个事件并正常结束。
- [ ] P95/P99 与错误率形成可重复的基线报告。
- [ ] 报告明确说明实例规格、测试时间和是否启用外部 LLM。

官方资料：[Render Service Metrics](https://render.com/docs/service-metrics)

## 6. P0：PostgreSQL 备份恢复演练

### 6.1 验证目标

证明备份不是只生成了一个文件，而是可以在隔离数据库中恢复并重新运行核心业务。

### 6.2 操作步骤

- [ ] 在 PostgreSQL A 中写入订单、审批、Agent Job、Saga、Outbox、Evidence 和 AuditLog。
- [ ] 使用项目脚本创建备份。
- [ ] 记录备份文件哈希和数据库时间点。
- [ ] 删除或破坏 PostgreSQL A 中的部分测试数据。
- [ ] 将备份恢复到隔离的 PostgreSQL B。
- [ ] 对 PostgreSQL B 执行 Alembic migration check。
- [ ] 校验关键表记录数量。
- [ ] 校验 Evidence 哈希链。
- [ ] 校验审批、Agent Job 和 Saga 状态。
- [ ] 用恢复数据库运行一条只读 Agent 流程。
- [ ] 记录 RPO 和 RTO。

项目命令：

```powershell
python backend/scripts/backup_restore.py backup --output-dir ./backups
python backend/scripts/backup_restore.py verify --backup ./backups/<backup-file>
python backend/scripts/backup_restore.py restore --backup <file> --target-url <url> --confirm-target <database-name>
```

### 6.3 应保存的证据

- [ ] 备份开始和结束时间。
- [ ] 备份文件 SHA-256。
- [ ] 恢复开始和结束时间。
- [ ] 实测 RPO 和 RTO。
- [ ] 恢复前后记录数量。
- [ ] Evidence 哈希链校验结果。
- [ ] 迁移版本和恢复后的 smoke test 结果。

### 6.4 可用于简历的表述

> 完成隔离环境备份恢复演练，验证审批任务、Saga、Outbox、Evidence 与审计记录恢复，并记录实测 RPO/RTO。

## 7. P1：真实 KMS 与密钥轮换

### 7.1 验证目标

将当前 KMS 抽象接入真实 AWS KMS、Azure Key Vault、GCP KMS 或 Vault Transit，证明主密钥不会进入数据库或应用配置。

推荐采用信封加密：

```text
KMS Master Key
  -> Generate/Wrap Data Key
  -> Data Key encrypts PII
  -> Database stores ciphertext + encrypted data key
```

### 7.2 操作步骤

- [ ] 创建一个测试用对称 KMS Key。
- [ ] 为测试身份配置最小权限。
- [ ] 禁止应用读取或导出主密钥材料。
- [ ] 使用随机 Data Key 加密 PII。
- [ ] 数据库只保存密文与 Encrypted Data Key。
- [ ] 启用自动轮换或执行一次按需轮换。
- [ ] 验证轮换前的数据仍可解密。
- [ ] 验证轮换后的新数据使用新密钥材料。
- [ ] 对旧 Encrypted Data Key 执行 rewrap 演练。
- [ ] 保存 CloudTrail 或同类审计记录。
- [ ] 验证无权限身份无法解密。

### 7.3 验收标准

- [ ] 主密钥没有写入 `.env`、数据库、日志或审计 Payload。
- [ ] 轮换前后数据均可按预期解密。
- [ ] 非授权身份解密失败并产生告警。
- [ ] 每次 KMS 操作具有可审计身份、时间和 Key ID。

官方资料：[AWS KMS Key Rotation](https://docs.aws.amazon.com/kms/latest/developerguide/rotate-keys.html)

## 8. P1：低成本真实用户 Shadow Pilot

### 8.1 验证目标

不接入企业生产系统，先验证目标用户是否能够理解、信任和使用 Agent 的判断、证据与审批建议。

### 8.2 用户与样本

- [ ] 招募 3-5 名做过财务、客服、采购、权限审批或企业 IT 的用户。
- [ ] 每人完成约 20 个脱敏或合成任务。
- [ ] 总样本目标为 60-100 个任务。
- [ ] Agent 只生成判断、证据、计划和审批建议。
- [ ] 所有写操作保持 Shadow Mode 或 Dry Run。
- [ ] 用户可以接受、拒绝、修改并说明原因。

### 8.3 指标定义

```text
任务完成率 = 满足成功标准的任务数 / 合格任务总数
人工接管率 = 转人工任务数 / 总任务数
人工修改率 = 人工修改 Agent 结论数 / 已审核任务数
建议接受率 = 直接接受 Agent 建议数 / 已审核任务数
节省工时 = 原人工中位耗时 - Agent 辅助中位耗时
审批 SLA = SLA 内完成审批数 / 审批任务数
错误成本 = 返工时间成本 + 错误操作损失 + 补偿成本
```

### 8.4 应保存的证据

- [ ] 脱敏后的任务输入。
- [ ] Agent TaskSpec、PlanGraph 和 Evidence。
- [ ] 用户的接受、拒绝和修改结果。
- [ ] 修改原因分类。
- [ ] 任务前后耗时。
- [ ] 失败案例和人工接管案例。
- [ ] Pilot 汇总报告和用户授权说明。

### 8.5 当前边界

Shadow Pilot 可以证明目标用户体验和决策辅助价值，但不能证明真实资金写入、生产 SAP 集成或正式企业 ROI。

## 9. P2：短期付费验证

在准备正式投递、作品展示或商业试点前，可以只短期开通一个月付费资源，不需要长期承担成本。

- [ ] Render 启动两个后端实例。
- [ ] 验证真实负载均衡下的跨实例 HITL 和幂等。
- [ ] 验证滚动部署与健康检查。
- [ ] 验证优雅关闭和在途任务恢复。
- [ ] 记录多实例 P50/P95/P99。
- [ ] 触发一次 PostgreSQL PITR 或逻辑备份恢复。
- [ ] 保存 Render Metrics、事件和部署时间线。

Render 多实例会按实例计费，自动扩缩容能力取决于工作区套餐。官方资料：[Render Scaling](https://render.com/docs/scaling)

## 10. 暂缓的企业依赖项

### 10.1 SAP 真实写入

需要真实 S/4HANA Cloud 测试租户、Communication Arrangement、授权用户和可清理的测试主数据。获得环境后再验证：

- Credit Memo Request 创建。
- CSRF Token。
- ETag 与 `If-Match`。
- 同步与异步处理。
- 重复请求幂等。
- 写入失败后的补偿和人工接管。

官方资料：[SAP Credit Memo Request OData V4](https://help.sap.com/docs/SAP_S4HANA_CLOUD/03c04db2a7434731b7fe21dca77440da/127e186758dd400cabd3a8af242c0fed.html)

### 10.2 Principal Propagation

需要 SAP BTP、企业 IdP、Destination、OAuth 2.0 SAML Bearer 或同类机制，以及 S/4HANA 用户映射。验收至少使用两个用户：

- 普通用户执行财务写操作被拒绝。
- 财务用户执行相同操作被允许。
- SAP 审计日志记录最终业务用户，而不是共享技术账号。

官方资料：[SAP Principal Propagation Tutorial](https://developers.sap.com/tutorials/abap-environment-business-partner-oauthsamlbearer.html)

### 10.3 正式合规与多区域灾备

独立开发阶段可以完成控制设计和演练，但不能自行宣称通过 SOC 2、ISO 27001、等保或企业合规验收。正式认证需要组织流程、人员、证据周期和第三方审计。

## 11. 建议告警清单

- [ ] HTTP 5xx 超过 1%。
- [ ] Agent TTFR 或 E2E P95 超过 SLO。
- [ ] KMS 解密失败。
- [ ] 数据库连接池使用率超过 80%。
- [ ] 审批 SLA 超时。
- [ ] Policy violation 大于 0。
- [ ] Saga 补偿失败大于 0。
- [ ] Outbox 死信持续积压。
- [ ] Evidence 哈希链校验失败。
- [ ] 服务、PostgreSQL 或 Redis 不可用。
- [ ] 单任务 Token 或成本超过预算。

## 12. 最终独立验证包

建议最终形成以下可展示产物：

```text
docs/reports/
  sap-contract-acceptance.json
  sap-field-mapping.md
  local-multi-instance-report.md
  render-load-report.json
  backup-restore-drill.md
  kms-rotation-report.md
  shadow-pilot-report.md
```

完成 P0 和 P1 后，可以将项目描述为“经过契约级外部系统验证、真实云环境压测、灾备演练、真实 KMS 验证和目标用户 Shadow Pilot 的企业 Agent 平台”。

在获得真实 SAP 测试租户前，应继续将以下能力标记为待企业环境验收：

- SAP 真实财务写入。
- Principal Propagation。
- 企业级多区域容量和灾难恢复。
- 正式安全与合规认证。
